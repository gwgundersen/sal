[project]
name = "sal"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["anthropic", "pymupdf", "mcp"]

[project.scripts]
sal = "sal:main"

[tool.setuptools]
py-modules = ["sal"]

[dependency-groups]
dev = ["pytest"]
